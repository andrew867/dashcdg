# Module: NB-IMA (narrowband baseline)

**Implementation (canonical):** `core/include/dashcdg/nb_ima_codec.h`, `core/src/nb_ima_codec.c`

**Wire:** v4 ids **2–7** today (same payload bytes for all narrowband ids until native modules replace per-id payloads).

**No `vendor/`** — first-party fixed-point C.
